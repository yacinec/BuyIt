import { connectDB } from "./connectDB.config";

import {
  PORT,
  DATABASE_URL,
  BCRYPT_SALT,
  ACCESS_TOKEN_INFO,
  REFRESH_TOKEN_INFO,
} from "./const.config";

export {
  connectDB,
  PORT,
  DATABASE_URL,
  BCRYPT_SALT,
  ACCESS_TOKEN_INFO,
  REFRESH_TOKEN_INFO,
};
